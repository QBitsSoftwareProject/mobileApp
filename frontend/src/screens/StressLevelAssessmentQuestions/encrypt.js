// import AES from 'react-native-aes-crypto';

// export const encryptData = async (props) => {
//   const message = props.text;
//   const key = await AES.randomKey(256); // Generate a random key
//   const iv = await AES.randomKey(128); // Generate a random IV

//   const encryptedMessage = await AES.encrypt(message, key, iv);

//   return encryptedMessage;
// };