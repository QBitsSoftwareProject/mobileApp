import { View, Text } from "react-native";
import React from "react";
import styles from "./style";
import { SafeAreaView } from "react-native-safe-area-context";

const EduContent = () => {
  return (
    <SafeAreaView>
      <Text>EduContent</Text>
    </SafeAreaView>
  );
};

export default EduContent;
